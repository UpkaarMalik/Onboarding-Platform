node_modules
dist
.env
*.log

# Uploaded files. Company policy PDFs, and — since joinee document
# upload — Aadhaar cards, PAN cards and bank details. A stray
# `git add -A` would commit identity documents into history permanently,
# where removing them means rewriting every commit since.
uploads/
tsconfig.build.tsbuildinfo
